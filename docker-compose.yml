version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: troc-frontend
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
